import template from './concentration.html';
import controller from './concentration.controller';
import './concentration.scss';

let concentrationComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default concentrationComponent;
